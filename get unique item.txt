# Input sets
set1 = {10, 20, 30, 40, 50}
set2 = {30, 40, 50, 60, 70}

# Combine the two sets using union operation to get unique items
unique_items = set1.union(set2)

# Output the result
print(unique_items)